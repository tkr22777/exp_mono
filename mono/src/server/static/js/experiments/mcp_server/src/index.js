import React from 'react';
import ReactDOM from 'react-dom/client';
import MCPServerExplorer from './components/MCPServerExplorer';

// Create the React root and render the MCP Server Explorer
const root = ReactDOM.createRoot(document.getElementById('react-mcp-server'));
root.render(<MCPServerExplorer />); 